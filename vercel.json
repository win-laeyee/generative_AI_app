{
    "builds": [
        {
            "src": "project/app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "project/app.py"
        }
    ]
}